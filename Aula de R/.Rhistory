#################################################################################
##########                     Exercicio 1                          #############
#################################################################################
#crie o vetor de nome "alfa" contendo as 5 primeiras consoantes do alfabeto
alfa<-("a","b","c","d","e")
alfa
#################################################################################
##########                     Exercicio 1                          #############
#################################################################################
#crie o vetor de nome "alfa" contendo as 5 primeiras consoantes do alfabeto
alfa<-c("a","b","c","d","e")
alfa
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<- integer(1,2,3,5,7)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<- integer c(1,2,3,5,7)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<-("integer",1,2,3,5,7)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<-("integer", 1)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<-("integer". 1)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<-c(1,2,3,5,7)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-(1,3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-[1,3,0.5]
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-(1:3.0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-(1:3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-c(1:3,0.5)
#crie um vetor numerico de 1 a 100
num<- (1:100)
num
#calcule a media
media<- median(num, na.rm=FALSE)
media
#transforme o vetor em uma matrix de 10x10
mat<-matrix(num,10,10)
View(mat)
#substitua a segunda coluna pela decima linha
mat[,2]<-10
mat
#substitua a segunda coluna pela decima linha
mat[,2]<-[10,]
#substitua a segunda coluna pela decima linha
mat[,2]<-10,
#substitua a segunda coluna pela decima linha
mat[,2]<-mat[10,]
mat
ls
num<- (1:100)
num
#calcule a media
media<- median(num, na.rm=FALSE)
media
#transforme o vetor em uma matrix de 10x10
mat<-matrix(num,10,10)
#substitua a segunda coluna pela decima linha
mat[,2]<-mat[10,]
mat
#calcule a media
media<-median(mat, na.rm=FALSE)
media
#calcule a media
media<- median(num, na.rm=FALSE)
media
#transforme o vetor em uma matrix de 10x10
mat<-matrix(num,10,10)
#calcule a media
media<-median(mat, na.rm=FALSE)
media
#substitua a segunda coluna pela decima linha
mat[,2]<-mat[10,]
mat
#calcule a media
media<-median(mat, na.rm=FALSE)
media
#verifique quantos valores da matrix "m" estao acima da media
mat>media
s
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
s<- sample(0:1000, 100)
s
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[87]
s[52]
#verifique o numero de duplicatas
duplicated(s)
#ordene o vetor s de forma crescente usando a funcao order(), criando o vetor "s3"
s3<- order(s)
s3
#sete a semente de numeros aleatorios em 1
set.seed(1)
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[87]   # ------------> Valor encontrado no which.max()
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#verifique o numero de duplicatas
duplicated(s)
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
set.seed(1)
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#verifique o numero de duplicatas
duplicated(s)
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
set.seed(1)
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#verifique o numero de duplicatas
duplicated(s)
#sete a semente de numeros aleatorios em 1
set.seed(1)
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#sete a semente de numeros aleatorios em 1
set.seed(2)
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
set.seed(1)
#sete a semente de numeros aleatorios em 1
set.seed(2)
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#verifique o numero de duplicatas
duplicated(s)
#verifique o numero de duplicatas
sum(duplicated(s), na.rm=FALSE)
#ordene o vetor s de forma crescente usando a funcao order(), criando o vetor "s3"
s3<- s[order(s)]
s3
#verifique o numero de duplicatas
soma<- sum(duplicated(s), na.rm=FALSE)
soma
#verifique quem sao as duplicatas
DJ<-c("na minha seed, não pareceu nenhuma duplicada", soma)
DJ
class(primos)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<-c(1L,2L,3L,5L,7L)
class(primos)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-c(1:3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-seq(1:3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-c(1,3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-c(1,3)+0.5
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-c(1:3)+0.5
num<- (1:100)
num
#calcule a media
media<- median(num, na.rm=FALSE)
media
#transforme o vetor em uma matrix de 10x10
mat<-matrix(num,10,10)
#substitua a segunda coluna pela decima linha
mat[,2]<-mat[10,]
mat
#calcule a media
media<-median(mat, na.rm=FALSE)
media
#verifique quantos valores da matrix "m" estao acima da media
mat>media
#sete a semente de numeros aleatorios em 1
set.seed(2)
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#verifique o numero de duplicatas
soma<- sum(duplicated(s), na.rm=FALSE)
soma
#verifique quem sao as duplicatas
DJ<-c("na minha seed, não pareceu nenhuma duplicada", soma)
DJ
getwd()
setwd(/home/djkf/Área de trabalho/MESTRADO/Aula de R)
setwd("/home/djkf/Área de trabalho/MESTRADO/Aula de R")
ls
#importe o arquivo network.txt para o R e crie um data.frame com nome "network"
load()
network<-read.table("network.txt")
View(network)
network<-read.table("network.txt", headr=T)
network<-read.table("network.txt", header=T)
View(network)
View(network)
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network<- network[(network$node1),(network$node2), (network$combined_score)]
help(NA)
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[c(network$node1),(network$node2), (network$combined_score)]
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[c(network$node1,network$node2, network$combined_score)]
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[c((network$node1),(network$node2), (network$combined_score))]
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[(network$node1),]
view(network1)
View(network1)
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[,(network$node1)]
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[network$node1,]
View(network1)
help("[]")
help([])
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[1,2,-1]
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[1,2]
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[(1,2),]
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[c(1,2),]
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[,c(1,2)]
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[,c(1,2,-1)]
#mantenha somente as informacoes das colunas $node1, $node2 e $combined_score
network1<- network[,c(1,2,15)]
#selecione somente as liogacoes com score acima de 0.700
network2<- network1(network1$combined_score>0.700)
#selecione somente as liogacoes com score acima de 0.700
network2<- network1[network1$combined_score>0.700]
#selecione somente as liogacoes com score acima de 0.700
network2<- network1$combined_score>0.700
#selecione somente as liogacoes com score acima de 0.700
network2<- network1(network1$combined_score>0.700)
#selecione somente as liogacoes com score acima de 0.700
network2<- network1[network1$combined_score>0.700]
#selecione somente as liogacoes com score acima de 0.700
network2<- nnetwork1$combined_score>0.700
#selecione somente as liogacoes com score acima de 0.700
network2<- network1$combined_score>0.700
#selecione somente as liogacoes com score acima de 0.700
network2<- network[network1$combined_score>0.700]
#selecione somente as liogacoes com score acima de 0.700
network2<- network[which(network1$combined_score>0.700)]
#selecione somente as liogacoes com score acima de 0.700
network2<- network1[which(network1$combined_score>0.700)]
#selecione somente as liogacoes com score acima de 0.700
network2<- network1[row(network1$combined_score>0.7)]
#selecione somente as liogacoes com score acima de 0.700
network2<- row(network1$combined_score>0.7)
#selecione somente as liogacoes com score acima de 0.700
network2<- network1[with(network1, (combined_score>0.700))]
#selecione somente as liogacoes com score acima de 0.700
network2<- network1[with(network1, (combined_score>0.700)),]
View(network2)
#identifique quantos e quais sao os nos da rede
quantos<- network2[unique(network2)]
#identifique quantos e quais sao os nos da rede
quantos<- network2[unique(c(network2$node1,network2$node2),]
#identifique quantos e quais sao os nos da rede
quantos<- network2[duplicated(c(network2$node1,network2$node2)),]
View(quantos)
View(quantos)
help("duplicated")
#identifique quantos e quais sao os nos da rede
quantos<- network2[duplicated(c(network2$node1,network2$node2),incomparables=F ),]
#identifique quantos e quais sao os nos da rede
quantos<- network2[duplicated(c(network2$node1,network2$node2),incomparables=T ),]
#identifique quantos e quais sao os nos da rede
A<- unique(network2$node1)
B<- unique(network2$node2)
#faca a funcao "K()" que calcule a conectividade de um determinado no da rede
# a funcao deve ter o argumento "symbol" e o argumento "network"
K<- function(symbol, network){
sum((network$node1== symbol),(network$node2==symbol))
}
K("NGF", network)  #ao rodar essa linha, a resposta devera ser 16
#faca a funcao "K()" que calcule a conectividade de um determinado no da rede
# a funcao deve ter o argumento "symbol" e o argumento "network"
K<- function(symbol, network){
sum((network2$node1== symbol),(network2$node2==symbol))
}
K("NGF", network)  #ao rodar essa linha, a resposta devera ser 16
vi<-sum((network$node1==symbol)[2]),(network$node2==symbol)[1]))
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinho<- function(symbol,network){
vi<-c(network$node1==symbol)[2],network$node2==symbol[1])
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinho<- function(symbol,network){
vi<-c(network$node1==symbol[2],network$node2==symbol[1])
vi
}
vizinhos("NGF", network) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network){
vi<-c(network$node1==symbol[2],network$node2==symbol[1])
vi
}
vizinhos("NGF", network) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network){
vi<-network$node1==symbol[2]
zi<-network$node2==symbol[1]
nhos<- c(vi,zi)
}
vizinhos("NGF", network) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
View(vizinhos)
View(vizinhos)
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network){
vi<-network$node1==symbol[2]
zi<-network$node2==symbol[1]
nhos<- c(vi,zi)
}
vizinhos("NGF", network) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network){
vi<-network$node1==symbol[2]
zi<-network$node2==symbol[1]
nhos<- c(vi,zi)
nhos
}
vizinhos("NGF", network) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network){
vi<-network(network$node1==symbol)[2]
zi<-network(network$node2==symbol)[1]
nhos<- c(vi,zi)
nhos
}
vizinhos("NGF", network) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network2){
vi<-network2(network2$node1==symbol)[2]
zi<-network2(network2$node2==symbol)[1]
nhos<- c(vi,zi)
nhos
}
vizinhos("NGF", network) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network2){
vi<-network2[network2$node1==symbol[2]]
zi<-network2[network2$node2==symbol[1]]
nhos<- c(vi,zi)
nhos
}
vizinhos("NGF", network) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network2){
vi<-network2[network2$node1==symbol[2],]
zi<-network2[network2$node2==symbol[1],]
nhos<- c(vi,zi)
nhos
}
vizinhos("NGF", network) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network2){
vi<-network2[network2$node1==symbol[2],]
zi<-network2[network2$node2==symbol[1],]
nhos<- c(vi,zi)
nhos
}
vizinhos("NGF", network2) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network2){
vi<-network2[,network2$node1==symbol[2]]
zi<-network2[,network2$node2==symbol[1]]
nhos<- c(vi,zi)
nhos
}
vizinhos("NGF", network2) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network){
vi<-network[,network$node1==symbol,[2]]
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network){
vi<-network[network$node1==symbol,[2]]
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network){
vi<-network[network$node1==symbol,]
zi<-network[network$node2==symbol,]
nhos<- c(vi,zi)
nhos
}
vizinhos("NGF", network2) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
###############################################################################
#faca a funcao "vizinhos()" para encontrar os vizinhos de um no na rede
vizinhos<- function(symbol,network){
vi<-network[network$node1==symbol,][2]
zi<-network[network$node2==symbol,][1]
nhos<- c(vi,zi)
nhos
}
vizinhos("NGF", network2) #ao rodar essa linha sua funcao deve retornar o resultado abaixo:
c<- unique(c(network2$node1, network2$node2))
c
quais<- unique(c(network2$node1, network2$node2))
quantos<- length(quais)
##############################################################################
#faca a funcao "Knetwork()" para encontrar a conectividade media da rede
Knetwork<- function(network){
nos<- nrow(network)
knetwork<- 2*(nos)/quantos
}
Knetwork(network)    ##ao rodar essa linha, a resposta devera ser  11.6319
##############################################################################
#faca a funcao "Knetwork()" para encontrar a conectividade media da rede
Knetwork<- function(network){
nos<- nrow(network)
k<- 2*(nos)/quantos
k
}
Knetwork(network)    ##ao rodar essa linha, a resposta devera ser  11.6319
##############################################################################
#faca a funcao "Knetwork()" para encontrar a conectividade media da rede
Knetwork<- function(network){
nos<- nrow(network)
k<- (2*nos)/quantos
k
}
Knetwork(network)    ##ao rodar essa linha, a resposta devera ser  11.6319
##############################################################################
#faca a funcao "Knetwork()" para encontrar a conectividade media da rede
Knetwork<- function(network){
nos<- nrow(quantos)
k<- (2*nos)/quantos
k
}
Knetwork(network)    ##ao rodar essa linha, a resposta devera ser  11.6319
##############################################################################
#faca a funcao "Knetwork()" para encontrar a conectividade media da rede
Knetwork<- function(network){
nos<- nrow(network2)
k<- (2*nos)/quantos
k
}
Knetwork(network)    ##ao rodar essa linha, a resposta devera ser  11.6319
as<-nrow(network2)/quantos(quantos-1)
as<-nrow(network2)/(quantos*(quantos-1))
