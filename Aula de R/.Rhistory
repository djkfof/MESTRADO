#################################################################################
##########                     Exercicio 1                          #############
#################################################################################
#crie o vetor de nome "alfa" contendo as 5 primeiras consoantes do alfabeto
alfa<-("a","b","c","d","e")
alfa
#################################################################################
##########                     Exercicio 1                          #############
#################################################################################
#crie o vetor de nome "alfa" contendo as 5 primeiras consoantes do alfabeto
alfa<-c("a","b","c","d","e")
alfa
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<- integer(1,2,3,5,7)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<- integer c(1,2,3,5,7)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<-("integer",1,2,3,5,7)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<-("integer", 1)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<-("integer". 1)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<-c(1,2,3,5,7)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-(1,3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-[1,3,0.5]
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-(1:3.0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-(1:3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-c(1:3,0.5)
#crie um vetor numerico de 1 a 100
num<- (1:100)
num
#calcule a media
media<- median(num, na.rm=FALSE)
media
#transforme o vetor em uma matrix de 10x10
mat<-matrix(num,10,10)
View(mat)
#substitua a segunda coluna pela decima linha
mat[,2]<-10
mat
#substitua a segunda coluna pela decima linha
mat[,2]<-[10,]
#substitua a segunda coluna pela decima linha
mat[,2]<-10,
#substitua a segunda coluna pela decima linha
mat[,2]<-mat[10,]
mat
ls
num<- (1:100)
num
#calcule a media
media<- median(num, na.rm=FALSE)
media
#transforme o vetor em uma matrix de 10x10
mat<-matrix(num,10,10)
#substitua a segunda coluna pela decima linha
mat[,2]<-mat[10,]
mat
#calcule a media
media<-median(mat, na.rm=FALSE)
media
#calcule a media
media<- median(num, na.rm=FALSE)
media
#transforme o vetor em uma matrix de 10x10
mat<-matrix(num,10,10)
#calcule a media
media<-median(mat, na.rm=FALSE)
media
#substitua a segunda coluna pela decima linha
mat[,2]<-mat[10,]
mat
#calcule a media
media<-median(mat, na.rm=FALSE)
media
#verifique quantos valores da matrix "m" estao acima da media
mat>media
s
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
s<- sample(0:1000, 100)
s
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[87]
s[52]
#verifique o numero de duplicatas
duplicated(s)
#ordene o vetor s de forma crescente usando a funcao order(), criando o vetor "s3"
s3<- order(s)
s3
#sete a semente de numeros aleatorios em 1
set.seed(1)
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[87]   # ------------> Valor encontrado no which.max()
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#verifique o numero de duplicatas
duplicated(s)
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
set.seed(1)
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#verifique o numero de duplicatas
duplicated(s)
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
set.seed(1)
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#verifique o numero de duplicatas
duplicated(s)
#sete a semente de numeros aleatorios em 1
set.seed(1)
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#sete a semente de numeros aleatorios em 1
set.seed(2)
#crie um vetor aleatorio "s" de de tamanho 100, com valores no intervalo de 0 a 1000, permitindo reposicao
set.seed(1)
#sete a semente de numeros aleatorios em 1
set.seed(2)
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#verifique o numero de duplicatas
duplicated(s)
#verifique o numero de duplicatas
sum(duplicated(s), na.rm=FALSE)
#ordene o vetor s de forma crescente usando a funcao order(), criando o vetor "s3"
s3<- s[order(s)]
s3
#verifique o numero de duplicatas
soma<- sum(duplicated(s), na.rm=FALSE)
soma
#verifique quem sao as duplicatas
DJ<-c("na minha seed, não pareceu nenhuma duplicada", soma)
DJ
class(primos)
#crie um integer de nome "primos" com os 5 primeiros numeros primos
primos<-c(1L,2L,3L,5L,7L)
class(primos)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-c(1:3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-seq(1:3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-c(1,3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-c(1,3)+0.5
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-c(1:3)+0.5
num<- (1:100)
num
#calcule a media
media<- median(num, na.rm=FALSE)
media
#transforme o vetor em uma matrix de 10x10
mat<-matrix(num,10,10)
#substitua a segunda coluna pela decima linha
mat[,2]<-mat[10,]
mat
#calcule a media
media<-median(mat, na.rm=FALSE)
media
#verifique quantos valores da matrix "m" estao acima da media
mat>media
#sete a semente de numeros aleatorios em 1
set.seed(2)
s<- sample(0:1000, 100)
s
#verifique a posicao dos valores maximos e minimos
which.max(s)
which.min(s)
s[84]   # ------------> Valor encontrado no which.max() no meu comando, foi somente para confirmação
s[67]   # ------------> Valor encontrado no which.min() no meu comando, foi somente para confirmação
#verifique o numero de duplicatas
soma<- sum(duplicated(s), na.rm=FALSE)
soma
#verifique quem sao as duplicatas
DJ<-c("na minha seed, não pareceu nenhuma duplicada", soma)
DJ
load("exer2.RData")
#Construa o data.frame "SH.DE" com os dados de "SH.limma", mantendo
#somente as sondas que estao presentes no data.frame "DE.exp"
SH.limma
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-seq(1:3,0.5)
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-seq(1,3,0.5)
help("seq")
#crie uma sequnecia de nome "x" de 1 a 3, com incremento de 0.5
x<-seq(1,3, by=0.5)
load("exer2.RData")
getwd()
getwd()
load("exer2.RData")
setwd("/home/djkf/Área de trabalho/MESTRADO/Aula de R")
getwd()
load("exer2.RData")
View(SH.limma)
View(SH.limma)
View(DEexp)
View(DEexp)
#Construa o data.frame "SH.DE" com os dados de "SH.limma", mantendo
#somente as sondas que estao presentes no data.frame "DE.exp"
intersect(colnames(SH.limma),calnames(DEexp))
#Construa o data.frame "SH.DE" com os dados de "SH.limma", mantendo
#somente as sondas que estao presentes no data.frame "DE.exp"
intersect(colnames(SH.limma),colnames(DEexp))
View(DEexp)
View(DEexp)
#Construa o data.frame "SH.DE" com os dados de "SH.limma", mantendo
#somente as sondas que estao presentes no data.frame "DE.exp"
intersect(rownames(SH.limma),rownames(DEexp))
#Construa o data.frame "SH.DE" com os dados de "SH.limma", mantendo
#somente as sondas que estao presentes no data.frame "DE.exp"
repetidos<-intersect(rownames(SH.limma),rownames(DEexp))
SH.DE<- marge(SH.limma, DEexp, by = repetidos, all.x=T)
SH.DE<- merge(SH.limma, DEexp, by = repetidos, all.x=T)
SH.DE<- merge(SH.limma, DEexp, by = "repetidos", all.x=T)
SH.DE<- merge(SH.limma, DEexp, by = "row.names", all.x=T)
View(SH.DE)
View(SH.DE)
#confirme se todos as sondas presentes em "DEexp" estao presentes em "SH.DE"
soma<- sum(SH.DE$Row.names%in%DEexp$Row.names)
#confirme se todos as sondas presentes em "DEexp" estao presentes em "SH.DE"
soma<- sum(SH.DE$Row.names%in%row.names(DEexp))
row(DEexp)
#confirme se todos as sondas presentes em "DEexp" estao presentes em "SH.DE"
soma<- sum(row.names(DEexp)%in%SH.DE$Row.names)
soma == nrow(DEexp)
#Elimine as linhas que contem NA do data.frame "SH.DE"
help(NA)
#Elimine as linhas que contem NA do data.frame "SH.DE"
SH.DE<-na.omit(SH.DE)
View(DEexp)
View(queryENSG)
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- cbind(SH.DE, ensemble_gene_id= queryENSG$ensembl_gene_id)
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
help("match")
SH.DE$ensemble_gene_id <- queryENSG$ensemble_gene_id[match(rownames(SH.DE), rownames(queryENSG))]
View(SH.DE)
View(SH.DE)
SH.DE$ensemble_gene_id <- queryENSG$ensemble_gene_id[match(rownames(SH.DE), rownames(queryENSG))]
View(SH.DE)
View(SH.DE)
SH.DE<- SH.DE$ensemble_gene_id <- queryENSG$ensemble_gene_id[match(rownames(SH.DE), rownames(queryENSG))]
SH.DE$ensemble_gene_id <- queryENSG$ensemble_gene_id[match(rownames(SH.DE), rownames(queryENSG))]
SH.DE<- merge(SH.limma, DEexp, by = "row.names", all.x=T)
#Elimine as linhas que contem NA do data.frame "SH.DE"
SH.DE<-na.omit(SH.DE)
View(SH.DE)
View(SH.DE)
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- append(SH.DE, queryENSG[1,] after = 36)
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- append(SH.DE, queryENSG[1,], after = 36)
View(SH.DE)
SH.DE<- merge(SH.limma, DEexp, by = "row.names", all.x=T)
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- cbind(SH.DE, queryENSG[1,])
View(SH.DE)
View(SH.DE)
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- cbind(SH.DE, queryENSG(1,))
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- cbind(SH.DE, queryENSG[[1,]]
View(SH.DE)
View(SH.DE)
View(SH.DE)
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<-queryENSG$ensembl_gene_id[match(SH.DE,queryENSG)]
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE$ensemble_gene_id<-queryENSG$ensembl_gene_id[match(SH.DE,queryENSG)]
SH.DE<- merge(SH.limma, DEexp, by = "row.names", all.x=T)
View(SH.limma)
View(queryENSG)
help("match")
ajshdy<- queryENSG[1,0]
View(ajshdy)
ajshdy<- queryENSG[1,]
View(ajshdy)
ajshdy<- queryENSG[,1]
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
help("paste")
as<- queryENSG[1,]
View(as)
as<- queryENSG[[1,]]
as<- queryENSG[,1]
as<- queryENSG$ensembl_gene_id
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- merge(SH.DE, queryENSG[ensemble_gene_id])
View(SH.DE)
View(SH.DE)
#faca uma lista "DEtime" com 5 data.frames, cada um contendo symbol,
#entrez e ensemble_gene_id dos genes upregulated nos tempos 6h, 1d, 2d, 3d e 5d, respectivamente
help("merge")
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- merge(SH.DE, queryENSG, by.x = "symbols", by.y= "hgnc_symbols", all = TRUE)
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- merge(SH.DE, queryENSG, by.x = symbols, by.y= hgnc_symbols, all = TRUE)
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- merge(SH.DE, queryENSG, by.x = "symbol", by.y= "hgnc_symbol", all = TRUE)
View(SH.DE)
View(SH.DE)
SH.DE<- merge(SH.limma, DEexp, by = "row.names", all.x=T)
#Elimine as linhas que contem NA do data.frame "SH.DE"
SH.DE<-na.omit(SH.DE)
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- merge(SH.DE, queryENSG, by.x = "symbol", by.y= "hgnc_symbol", all = TRUE)
View(SH.DE)
#faca uma lista "DEtime" com 5 data.frames, cada um contendo symbol,
#entrez e ensemble_gene_id dos genes upregulated nos tempos 6h, 1d, 2d, 3d e 5d, respectivamente
DEtime<- c("symbol","entrez", "ensemble_gene_id")
df_6h<- SH.DE[SH.DE$logFC.t6h...t0h>0, DEtime]
df_6h<- SH.DE[which(SH.DE$logFC.t6h...t0h>0), DEtime]
df_6h<- SH.DE[which(SH.DE$logFC.t6h...t0h>0), c("symbol", "entrez", "ensemble_gene_id")]
#Adicione uma coluna em "SH.DE" contendo os dados de "ensemble_gene_id"
#contidos no data.frame "queryENSG"
SH.DE<- merge(SH.DE, queryENSG, by.x = "Row.names", by.y= "hgnc_symbol", all = TRUE)
df_6h<- SH.DE[which(SH.DE$logFC.t6h...t0h>0), c("symbol", "entrez", "ensemble_gene_id")]
df_6h<- SH.DE[which(SH.DE$logFC.t6h...t0h>0), c("symbol", "entrez", "ensembl_gene_id.y")]
df_1d<- SH.DE[which(SH.DE$logFC.t1d...t0h>0), c("symbol", "entrez", "ensembl_gene_id.y")]
df_2d<- SH.DE[which(SH.DE$logFC.t2d...t0h>0), c("symbol", "entrez", "ensembl_gene_id.y")]
df_3d<- SH.DE[which(SH.DE$logFC.t3d...t0h>0), c("symbol", "entrez", "ensembl_gene_id.y")]
df_5d<- SH.DE[which(SH.DE$logFC.t5d...t0h>0), c("symbol", "entrez", "ensembl_gene_id.y")]
DEtime<- list(df_6h,df_1d,df_2d,df_3d,df_5d)
